# Minimal Neovim Setup

A much cleaner Neovim setup that uses bare minimum plugins required for my dev workflow. It is built using lazy packer manager for managing plugins.
Also contains a minimal TMUX config file. 
